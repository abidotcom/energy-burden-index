#!/bin/bash
# Run this once to set up your GitHub repo and push everything

# 1. Initialize git
git init
git add .
git commit -m "Initial commit — EBI model, Streamlit dashboard, README"

# 2. Create repo on GitHub (do this manually at github.com first, then paste your repo URL below)
# git remote add origin https://github.com/YOUR_USERNAME/energy-burden-index.git

# 3. Push
# git branch -M main
# git push -u origin main

echo "Done! Uncomment lines 9-12 after creating your GitHub repo."
